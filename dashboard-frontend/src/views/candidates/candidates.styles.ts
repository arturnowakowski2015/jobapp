import { SxProps, Theme } from '@mui/material';

export const aboveTableContainer: SxProps<Theme> = {
  display: 'flex',
  alignItems: 'center',
  columnGap: 2,
};

export const addButton: SxProps<Theme> = {
  marginLeft: 'auto',
};
